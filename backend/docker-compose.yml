version: "3.8"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ship0-backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "4000:4000"