version: "3.8"

services:
  ship0-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ship0-backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "4001:4001"
